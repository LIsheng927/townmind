feat(evals): LLM-as-judge 第二套判定——幻觉累积结论站住，传闻抑制结论缩水

evals/judge.py：比 NPC 强一档的模型（gpt-4o 判 gpt-4o-mini）按标签定义打标 + 一句理由，
走 tool calling 枚举。hallucination_recall / hearsay 加 --judge，两套判定并列，附一致率和
Cohen's κ，不一致的单独列出。

真实数据：
  幻觉累积（n=96）κ=0.66。新提示词主动纠正 77%/77%，顺着编 0%/2%——结论不变。
  传闻抑制（每档 n=30）复述型 κ=0.22、出处型 0.47。关键词系统性漏判：HEDGE 只认连写的
  "听说"，"听 Greta 说"就漏；UNSURE_SOURCE 只有"也是听来的"，"这是听来的"就漏。漏的
  都在基线那侧，把提升放大。按裁判重算：保留不确定措辞 80%→100%（曾写 47%→80%），
  交代出处 100%→100%（曾写 80%→100%，纯漏判制造）。README 总表、传闻抑制节、方法论节
  相应修正，旧表保留作对照。

没有回头补关键词：看了裁判再改就是对着样本过拟合，两套判定要独立。裁判也不是真理，
Carol 那条它判错了，README 里写明。

测试 +4（κ 算法、枚举工具、失败退化）。
